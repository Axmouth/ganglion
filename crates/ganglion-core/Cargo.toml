[package]
name = "ganglion-core"
version = "0.1.0"
edition = "2021"
description = "Core types and traits for the ganglion cluster coordination engine."
license = "MIT"
repository = "https://github.com/Axmouth/ganglion"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
proptest = "1"
